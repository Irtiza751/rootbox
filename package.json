{
  "name": "rootbox",
  "version": "1.0.0",
  "description": "Rootbox",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "build:rootbox": "turbo run build --filter=rootbox",
    "build:playground": "turbo run build --filter=playground",
    "dev:rootbox": "turbo run dev --filter=rootbox",
    "dev:playground": "turbo run dev --filter=playground",
    "clean": "turbo run clean --filter=rootbox",
    "publish": "turbo run build lint test && changeset version && changeset publish"
  },
  "keywords": [],
  "author": "Muhammad Irtiza",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^2.0.6"
  },
  "packageManager": "pnpm@8.12.1",
  "dependencies": {
    "@changesets/cli": "^2.27.7"
  }
}
